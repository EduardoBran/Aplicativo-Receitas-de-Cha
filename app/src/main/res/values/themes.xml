<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base do app (MATERIAL COMPONENTS, modo claro) -->
    <style name="Theme.AppReceitasCha" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Cores principais mapeadas em colors.xml -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>

        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/colorOnError</item>

        <!-- Fundo padrão da janela -->
        <item name="android:windowBackground">@color/app_background</item>

        <!-- Edge-to-edge + barras claras (ícones escuros) no modo claro -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <!-- Demais tipografias/heranças seguem do MaterialComponents -->
    </style>

    <!-- Estilo base para o BottomNavigationView do app -->
    <style name="Widget.AppReceitasCha.BottomNav" parent="Widget.MaterialComponents.BottomNavigationView" />

    <!-- Indicador ativo do BottomNavigationView em forma de pílula -->
    <style name="Widget.AppReceitasCha.BottomNav.ActiveIndicator">
        <item name="android:width">72dp</item>
        <item name="android:height">40dp</item>
        <!-- Forma de pílula totalmente arredondada -->
        <item name="shapeAppearance">@style/ShapeAppearance.AppReceitasCha.BottomNav.Pill</item>
        <item name="android:color">@color/bottom_nav_active_indicator</item>
    </style>

    <style name="ShapeAppearance.AppReceitasCha.BottomNav.Pill" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>